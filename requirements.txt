numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib
seaborn
joblib
tensorflow>=2.15.0
shap>=0.44.0
streamlit>=1.30.0
pytest>=7.4.0
# TabNet is optional. If installation fails on your machine, the project still
# works fully with DNN + MLP (TabNet is skipped automatically, not required).
torch
pytorch-tabnet
